# 财务分析工具重构版 v17 - 重建计划

**创建日期：** 2026-05-21  
**基于版本：** archive/index-complete.html  
**目标版本：** 符合"完整逻辑架构.md"文档要求

---

## 📋 核心改动清单

### 1. Tab结构调整

**当前Tab结构：**
- 📊 经营概况 (overview)
- 🔍 详细分析 (detail)
- 🩺 诊断中心 (diagnosis)
- 💰 成本分析 (cost)
- 📋 分析报告 (report)

**目标Tab结构：**
- 📊 概览 (overview)
- 📐 维度下钻 (dimension)
- 📈 趋势分析 (trend)
- 🎯 敏感性分析 (sensitivity)
- 💡 智能分析 (insights)

### 2. 各Tab内容调整

#### Tab1: 概览 (overview)
**保持不变**，已有完整的：
- 核心指标卡片
- 城市UE对比图表
- 成本构成图表
- 10城x5模块UE矩阵
- 异常预警
- 智能洞察

#### Tab2: 维度下钻 (dimension) - **需要新建**
**功能：** 多维度对比分析

**组成：**
- 城市选择器（最多3个，默认选中：总商、承德、玉田）
- 业务板块选择器（全部/餐饮/闪购/医药/拼好饭）
- 商家类型选择器（全部/KA/城市商家）
- 指标分组表格（订单、GMV、收入、成本、利润）
- 可折叠的指标层级

**数据展示：**
- 多城市并排对比
- 按指标分组展示
- 支持展开/折叠下级指标

#### Tab3: 趋势分析 (trend) - **需要新建/改造**
**功能：** 时间维度分析

**组成：**
- 周度趋势分析（近4周）
- 月度趋势分析（当前月）
- 指标分组展示
- 城市对比
- 图表可视化

**数据范围：**
- 4月：完整月度 + 近4周
- 5月：最近一期月度

#### Tab4: 敏感性分析 (sensitivity) - **需要新建**
**功能：** 参数调整模拟

**组成：**
- 城市选择器（单城市）
- 参数滑块：
  - 业务结构（餐饮/闪购/医药/拼好饭占比）
  - 商家结构（KA/城市商家占比）
  - 配送方式（加盟/众包/悦跑占比）
  - 收入结构（抽佣/配送费/其他占比）
  - 补贴策略（B端/C端补贴率）
- 原始数据 vs 模拟数据对比
- 详细对比表格

**计算逻辑：**
- 调整参数 → 重新计算 → 实时更新结果
- 显示各指标变化幅度

#### Tab5: 智能分析 (insights) - **改造自diagnosis**
**功能：** 综合分析和建议

**组成：**
- 健康度仪表盘（综合评分）
- 异常检测列表（按严重程度排序）
- 成本结构分析
- 量化建议（P0/P1优先级）
- 一键生成报告

---

## 🔧 技术实现细节

### 1. 数据结构调整

```javascript
currentData = {
  totals: {         // 总商数据
    orders: 850000,
    gmvAmount: 12500000,
    onlineRevenue: 17393640.34,
    profit: 368287.10,
    ue: 0.193,
    profitRate: 0.021,
    subsidyRatio: 0.05,
    // ... 其他指标
  },
  cities: [         // 各城市数据
    {
      name: '承德',
      modules: {
        all: { orders, gmvAmount, ... },      // 全品类
        food: { orders, gmvAmount, ... },     // 餐饮
        flash: { orders, gmvAmount, ... },    // 闪购
        medicine: { orders, gmvAmount, ... }, // 医药
        group: { orders, gmvAmount, ... }     // 拼好饭
      }
    },
    // ... 其他城市
  ]
};
```

### 2. 核心函数

```javascript
// 入口函数
window.onload = function() {
  const savedData = loadFromLocalStorage();
  if (savedData) {
    currentData = savedData;
    renderOverview(currentData);
    renderDimensionDashboard();
  } else {
    setTimeout(loadDemoData, 500);
  }
};

// Tab切换
function switchTab(tabName) {
  // 切换Tab样式
  document.querySelectorAll('.nav-tab').forEach(t => t.classList.remove('active'));
  document.querySelector(`.nav-tab[onclick="switchTab('${tabName}')"]`).classList.add('active');
  
  // 切换内容区域
  document.querySelectorAll('.tab-content').forEach(t => t.classList.remove('active'));
  document.getElementById(`tab-${tabName}`).classList.add('active');
  
  // 延迟初始化对应Tab
  setTimeout(() => {
    if (tabName === 'dimension') renderDimensionDrilldown();
    if (tabName === 'trend') renderTrend();
    if (tabName === 'sensitivity') initSensitivity();
    if (tabName === 'insights') renderInsights();
  }, 100);
}
```

### 3. 关键渲染函数

```javascript
// 概览Tab
function renderOverview(data) {
  // 核心指标卡片
  // 健康度仪表盘
  // 异常检测
  // 关键洞察
}

// 维度下钻Tab
function renderDimensionDrilldown() {
  // 城市选择器
  // 业务板块选择器
  // 多城市对比表格
  // 可折叠的指标层级
}

// 趋势分析Tab
function renderTrend() {
  // 周度趋势
  // 月度趋势
  // 指标分组展示
}

// 敏感性分析Tab
function initSensitivity() {
  // 城市选择器
  // 参数滑块
  // 实时计算
  // 对比表格
}

// 智能分析Tab
function renderInsights() {
  // 健康度仪表盘
  // 异常检测
  // 成本结构
  // 量化建议
}
```

---

## 📁 文件位置

**输出文件：** `index.html`（重构版 v17.1）  
**备份文件：** `index-v17-refactor-备份.html`

---

## ⚠️ 注意事项

1. **保持向后兼容**：新版本需要能读取旧版本的数据格式
2. **性能优化**：大数据量时需要防抖和节流
3. **响应式设计**：支持移动端查看
4. **错误处理**：所有函数都需要异常捕获
5. **数据持久化**：每次数据变更自动保存到LocalStorage

---

## ✅ 验收标准

1. ✅ 5个Tab可以正常切换
2. ✅ 数据可以正常导入和显示
3. ✅ 维度下钻：可以选择多个城市对比
4. ✅ 趋势分析：可以查看周度和月度趋势
5. ✅ 敏感性分析：可以调整参数并实时查看结果
6. ✅ 智能分析：可以查看健康度和异常建议
7. ✅ LocalStorage持久化正常工作
8. ✅ 无严重JS错误或控制台警告

---

**下一步工作：**
1. 修改Tab结构和名称
2. 新建/改造各Tab的内容实现
3. 完善数据结构和函数
4. 测试验证

