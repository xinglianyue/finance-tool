# 财务分析工具 - 快速交接

## 🎯 当前状态
- **版本：** v2.0 重构版
- **主文件：** `archive/index-refactor.html`
- **状态：** 功能框架完成，**真实数据解析需要修正**

## ✅ 已完成
1. 5个Tab功能（概览、维度下钻、趋势分析、敏感性分析、智能分析）
2. 数据导入/导出/持久化功能框架
3. XLSX库已引入

## ⚠️ 需要修正的问题

### 核心问题：parseExcelData()函数（第757-843行）
**问题：** 使用了固定的行列索引，不够灵活
**解决方案：** 对照 `js/parser.js` 重写，使用动态匹配逻辑

### 具体修正点：

#### 1. 城市列索引错误
```javascript
// ❌ 错误：从第3列开始
cities: cityRow.slice(3)

// ✅ 正确：从第5列开始
for (let col = 5; col < cityRow.length; col++)
```

#### 2. 需要动态搜索模块位置
```javascript
// ❌ 错误：固定行号
getValue(10, colIdx) // 订单量在第10行

// ✅ 正确：动态搜索模块行
const modulePositions = {};
for (let rn = 0; rn < data.length; rn++) {
  const row = data[rn];
  if (row[0] === '全品类财务指标') modulePositions['all'] = rn;
  if (row[0] === '餐饮财务指标') modulePositions['food'] = rn;
  // ...
}
```

#### 3. 解析多模块数据
```javascript
// 需要解析5个模块
const MODULE_DEFS = [
  { key: 'all', label: '全品类' },
  { key: 'food', label: '餐饮' },
  { key: 'flash', label: '闪购' },
  { key: 'medicine', label: '医药' },
  { key: 'group', label: '拼好饭' }
];

// 每个城市要有这5个模块的数据
city.modules = {
  all: { orders, gmvAmount, ... },
  food: { orders, gmvAmount, ... },
  // ...
}
```

## 📋 修正步骤

### Step 1：复制 js/parser.js 的逻辑
直接复制 `js/parser.js` 第6-215行的 `parseExcelFile` 函数到 `index-refactor.html`

### Step 2：适配新的调用方式
```javascript
function importData(event) {
  // ... 现有代码 ...
  
  // 替换这部分
  // const parsedData = parseExcelData(jsonData);
  // 改为：
  const merchantData = parseExcelFile(workbook);
  const parsedData = convertToAppFormat(merchantData);
  
  if (parsedData) {
    currentData = parsedData;
    // ...
  }
}

function convertToAppFormat(merchantData) {
  // 从 merchantData['all'].cities 转换
  // 到 { totals, cities: [...] }
}
```

### Step 3：测试
1. 打开 http://localhost:8080/archive/index-refactor.html
2. 点击"📥 导入数据"
3. 选择 `C:/Users/xinxi/Desktop/美团工作/外卖账单20260415.xlsx`
4. 检查数据是否正确显示
5. 对比 index-v2.html 的结果

## 🔧 快速测试命令
```bash
# 启动服务器
cd C:/Users/xinxi/Desktop/财务工具
python -m http.server 8080

# 或使用已有的启动脚本
双击"启动服务器.bat"
```

## 📂 关键文件
- **主文件：** [archive/index-refactor.html](file:///c:/Users/xinxi/Desktop/财务工具/archive/index-refactor.html)
- **参考逻辑：** [js/parser.js](file:///c:/Users/xinxi/Desktop/财务工具/js/parser.js)
- **测试数据：** `C:/Users/xinxi/Desktop/美团工作/外卖账单20260415.xlsx`

---

**下一步：** 修正 parseExcelData() 函数 → 测试真实数据导入 → 验证数据正确性 → 代码重构优化
